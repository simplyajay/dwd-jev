DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="changeme"
PORT=3000
# Optional -- used only by prisma/seed.ts. If unset, the seed script
# generates and prints a random password instead.
ADMIN_PASSWORD=
